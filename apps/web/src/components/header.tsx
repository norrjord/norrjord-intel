"use client";

// Header is no longer used — the CRM layout has its own sidebar navigation.
// Kept as an empty export to avoid breaking any remaining imports.
export default function Header() {
  return null;
}
